export const ROLE_PARAMETRIZATIONS = String(
  `${import.meta.env.VITE_APP_ROLE_PARAMETRIZATIONS}`
)

export const ROLE_MULTIVIEW_POINT = String(
  `${import.meta.env.VITE_APP_ROLE_MULTIVIEW_POINT}`
)

export const ROLE_TEAM_POINT = String(
  `${import.meta.env.VITE_APP_ROLE_TEAM_POINT}`
)
